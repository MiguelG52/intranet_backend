doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Restablecer Contraseña
    style.
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');
      *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: linear-gradient(145deg, #ffffff 0%, #f7f8fc 50%, #fdf4f4 100%);
        padding: 20px 16px;
      }
      .wrapper {
        max-width: 560px; margin: 0 auto; position: relative;
      }
      .card {
        background: rgba(255,255,255,0.82);
        border: 1px solid rgba(255,255,255,0.95);
        border-radius: 24px;
        box-shadow:
          0 12px 48px rgba(0,0,0,0.08),
          0 2px 4px rgba(0,0,0,0.04),
          inset 0 1px 0 rgba(255,255,255,1);
        overflow: hidden;
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
      }
      .card-stripe {
        height: 4px;
        background: linear-gradient(90deg, #b71c1c 0%, #e53935 55%, #ef9a9a 100%);
      }
      .card-body {
        padding: 44px 44px 36px;
        text-align: center;
      }
      .header-eyebrow {
        display: inline-block;
        font-size: 10px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #e53935; margin-bottom: 14px;
      }
      .title {
        font-size: 30px; font-weight: 800;
        color: #111827;
        letter-spacing: -0.6px;
        line-height: 1.2;
        margin-bottom: 4px;
      }
      .title span { color: #e53935; }
      .greeting {
        font-size: 16px; color: #374151;
        margin-bottom: 14px; margin-top: 20px;
        font-weight: 400;
      }
      .greeting strong { color: #111827; font-weight: 700; }
      .description {
        font-size: 14px; color: #6b7280; line-height: 1.75;
        max-width: 380px; margin: 0 auto 32px;
      }
      .btn-wrap { margin-bottom: 36px; }
      .btn {
        display: inline-block;
        background: linear-gradient(135deg, #c62828 0%, #e53935 100%);
        color: #ffffff !important;
        text-decoration: none;
        font-size: 14px; font-weight: 700;
        padding: 14px 44px;
        border-radius: 50px;
        box-shadow: 0 6px 22px rgba(198,40,40,0.38), inset 0 1px 0 rgba(255,255,255,0.18);
        letter-spacing: 0.3px;
      }
      hr.div {
        border: none;
        border-top: 1px solid rgba(0,0,0,0.06);
        margin: 0 0 28px;
      }
      .validity-badge {
        display: inline-flex; align-items: center; gap: 10px;
        background: rgba(255,235,238,0.7);
        border: 1px solid rgba(229,57,53,0.18);
        border-radius: 50px;
        padding: 9px 22px;
        margin-bottom: 18px;
        box-shadow: inset 0 1px 0 rgba(255,255,255,0.9);
      }
      .validity-dot {
        width: 7px; height: 7px;
        background: #e53935; border-radius: 50%; flex-shrink: 0;
      }
      .validity-text {
        font-size: 13px; font-weight: 700; color: #e53935;
        letter-spacing: 0.2px;
      }
      .ignore-text {
        font-size: 13px; color: #9ca3af; line-height: 1.65;
        max-width: 340px; margin: 0 auto;
      }
      .card-footer {
        border-top: 1px solid rgba(0,0,0,0.05);
        padding: 20px 44px 28px;
        text-align: center;
      }
      .footer-text { font-size: 12px; color: #9ca3af; line-height: 1.7; }
      .footer-text a { color: #e53935; text-decoration: none; }
      .brand {
        font-size: 11px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #d1d5db; margin-top: 10px;
      }
  body
    .wrapper
      .card
        .card-stripe
        .card-body
          p.header-eyebrow Seguridad de cuenta
          h1.title Restablecer
          h1.title
            span Contraseña
          p.greeting Hola, <strong>#{name}</strong>.
          p.description
            | Recibimos una solicitud para cambiar tu contraseña.
            | Haz clic en el botón de abajo para crear una nueva.
          .btn-wrap
            a.btn(href=resetUrl target="_blank") Restablecer mi contraseña
          hr.div
          .validity-badge
            .validity-dot
            span.validity-text Enlace válido por 30 minutos
          p.ignore-text
            | Si no solicitaste este cambio, puedes ignorar este correo de manera segura.
        .card-footer
          p.footer-text
            | ¿Necesitas ayuda? Contáctanos en&nbsp;
            a(href="mailto:soporte@ashasolution.com") soporte@ashasolution.com
          p.brand ASHA Solution
