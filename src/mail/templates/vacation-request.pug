doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Nueva Solicitud de Vacaciones
    style.
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');
      *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: linear-gradient(145deg, #ffffff 0%, #f7f8fc 50%, #fdf4f4 100%);
        padding: 20px 16px;
      }
      .wrapper { max-width: 560px; margin: 0 auto; position: relative; }
      .card {
        background: rgba(255,255,255,0.82);
        border: 1px solid rgba(255,255,255,0.95);
        border-radius: 24px;
        box-shadow:
          0 12px 48px rgba(0,0,0,0.08),
          0 2px 4px rgba(0,0,0,0.04),
          inset 0 1px 0 rgba(255,255,255,1);
        overflow: hidden;
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
      }
      .card-stripe {
        height: 4px;
        background: linear-gradient(90deg, #b71c1c 0%, #e53935 55%, #ef9a9a 100%);
      }
      .card-header {
        padding: 36px 44px 28px;
        text-align: center;
        border-bottom: 1px solid rgba(0,0,0,0.05);
      }
      .header-eyebrow {
        display: inline-block;
        font-size: 10px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #e53935; margin-bottom: 10px;
      }
      .header-title {
        font-size: 30px; font-weight: 800;
        color: #111827; letter-spacing: -0.6px;
        line-height: 1.2;
      }
      .header-title span { color: #e53935; }
      .header-sub { font-size: 13px; color: #9ca3af; margin-top: 6px; }
      .card-body { padding: 32px 44px; }
      .greeting {
        font-size: 17px; font-weight: 600; color: #111827;
        margin-bottom: 10px;
      }
      .intro-text {
        font-size: 14px; color: #6b7280; line-height: 1.75;
        margin-bottom: 28px;
      }
      .section-label {
        font-size: 9px; font-weight: 700;
        color: #e53935; letter-spacing: 2px;
        text-transform: uppercase; margin-bottom: 12px;
      }
      .glass-info-card {
        background: rgba(255,255,255,0.75);
        border: 1px solid rgba(229,57,53,0.10);
        border-radius: 16px;
        padding: 20px 22px;
        margin-bottom: 20px;
        box-shadow: inset 0 1px 0 rgba(255,255,255,1), 0 4px 16px rgba(229,57,53,0.05);
      }
      .info-grid {
        display: grid; grid-template-columns: 1fr 1fr;
        gap: 18px;
      }
      .info-label {
        font-size: 9px; font-weight: 600;
        color: #9ca3af; letter-spacing: 1px;
        text-transform: uppercase; margin-bottom: 4px;
      }
      .info-value {
        font-size: 15px; font-weight: 700; color: #111827;
      }
      .info-value.accent { color: #e53935; }
      .days-pill {
        display: inline-block;
        background: rgba(229,57,53,0.08);
        border: 1px solid rgba(229,57,53,0.15);
        border-radius: 50px;
        padding: 3px 12px;
        font-size: 14px; font-weight: 800;
        color: #c62828;
      }
      .status-row {
        display: flex; align-items: center; gap: 10px;
        margin-bottom: 20px;
      }
      .status-dot {
        width: 8px; height: 8px;
        background: #f59e0b;
        border-radius: 50%;
        box-shadow: 0 0 0 3px rgba(245,158,11,0.2);
        flex-shrink: 0;
      }
      .status-label { font-size: 13px; color: #78350f; font-weight: 600; }
      .glass-notes {
        background: rgba(255,248,230,0.7);
        border: 1px solid rgba(251,191,36,0.2);
        border-left: 3px solid #f59e0b;
        border-radius: 12px;
        padding: 12px 16px;
        margin-bottom: 24px;
      }
      .notes-label {
        font-size: 9px; font-weight: 700;
        color: #92400e; letter-spacing: 1.5px;
        text-transform: uppercase; margin-bottom: 6px;
      }
      .notes-text { font-size: 13px; color: #6b7280; line-height: 1.6; font-style: italic; }
      .btn-wrap { text-align: center; margin-bottom: 8px; }
      .btn {
        display: inline-block;
        background: linear-gradient(135deg, #c62828 0%, #e53935 100%);
        color: #ffffff !important;
        text-decoration: none;
        font-size: 14px; font-weight: 700;
        padding: 14px 40px;
        border-radius: 50px;
        box-shadow: 0 6px 22px rgba(198,40,40,0.38), inset 0 1px 0 rgba(255,255,255,0.18);
        letter-spacing: 0.3px;
      }
      hr.div { border: none; border-top: 1px solid rgba(0,0,0,0.06); margin: 24px 0 20px; }
      .card-footer {
        padding: 20px 44px 28px;
        text-align: center;
        border-top: 1px solid rgba(0,0,0,0.05);
      }
      .footer-text { font-size: 12px; color: #9ca3af; line-height: 1.7; }
      .footer-text a { color: #e53935; text-decoration: none; }
      .brand {
        font-size: 11px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #d1d5db; margin-top: 10px;
      }
  body
    .wrapper
      .card
        .card-stripe
        .card-header
          p.header-eyebrow Nueva solicitud
          h1.header-title Solicitud de
          h1.header-title
            span Vacaciones
          p.header-sub Enviada el #{requestDate}
        .card-body
          p.greeting Hola, #{recipientName}
          p.intro-text
            | Se ha registrado una nueva solicitud de vacaciones.
            | A continuación encontrarás los detalles completos.
          p.section-label Detalles de la solicitud
          .glass-info-card
            .info-grid
              .info-item
                p.info-label Solicitante
                p.info-value #{employeeName}
              .info-item
                p.info-label Días solicitados
                p.info-value
                  span.days-pill #{totalDays} días
              .info-item
                p.info-label Fecha de inicio
                p.info-value.accent #{startDate}
              .info-item
                p.info-label Fecha de fin
                p.info-value.accent #{endDate}
          .status-row
            .status-dot
            p.status-label Pendiente de aprobación
          if notes
            .glass-notes
              p.notes-label Notas del solicitante
              p.notes-text #{notes}
          .btn-wrap
            a.btn(href=requestUrl target="_blank") Ver solicitud completa
          hr.div
        .card-footer
          p.footer-text
            | Este correo fue generado automáticamente.
            | &nbsp;
            a(href=requestUrl) Ver en el portal
            | &nbsp;·&nbsp;
            a(href="mailto:soporte@ashasolution.com") soporte@ashasolution.com
          p.brand ASHA Solution
