doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Bienvenido a ASHA Solution
    style.
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');
      *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: linear-gradient(145deg, #ffffff 0%, #f7f8fc 50%, #fdf4f4 100%);
        padding: 20px 16px;
      }
      .wrapper {
        max-width: 560px; margin: 0 auto; position: relative;
      }
      .card {
        background: rgba(255,255,255,0.82);
        border: 1px solid rgba(255,255,255,0.95);
        border-radius: 24px;
        box-shadow:
          0 12px 48px rgba(0,0,0,0.08),
          0 2px 4px rgba(0,0,0,0.04),
          inset 0 1px 0 rgba(255,255,255,1);
        overflow: hidden;
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
      }
      .card-stripe {
        height: 4px;
        background: linear-gradient(90deg, #b71c1c 0%, #e53935 55%, #ef9a9a 100%);
      }
      .card-header {
        padding: 36px 44px 28px;
        text-align: center;
        border-bottom: 1px solid rgba(0,0,0,0.05);
      }
      .header-eyebrow {
        display: inline-block;
        font-size: 10px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #e53935; margin-bottom: 10px;
      }
      .header-title {
        font-size: 30px; font-weight: 800;
        color: #111827; letter-spacing: -0.6px;
        line-height: 1.2;
      }
      .header-title span { color: #e53935; }
      .header-sub {
        font-size: 14px; color: #9ca3af; margin-top: 8px;
      }
      .card-body { padding: 32px 44px; }
      .greeting {
        font-size: 17px; font-weight: 600; color: #111827;
        margin-bottom: 10px;
      }
      .body-text {
        font-size: 14px; color: #6b7280; line-height: 1.75;
        margin-bottom: 28px;
      }
      .glass-box {
        background: rgba(255,255,255,0.75);
        border: 1px solid rgba(229,57,53,0.12);
        border-radius: 16px;
        padding: 20px 22px;
        margin-bottom: 28px;
        box-shadow: inset 0 1px 0 rgba(255,255,255,1), 0 4px 16px rgba(229,57,53,0.06);
      }
      .box-label {
        font-size: 9px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #e53935; margin-bottom: 16px;
      }
      .cred-row {
        display: flex; align-items: flex-start; gap: 12px;
        margin-bottom: 14px;
      }
      .cred-row:last-child { margin-bottom: 0; }
      .cred-dot {
        width: 6px; height: 6px;
        background: #e53935; border-radius: 50%;
        margin-top: 6px; flex-shrink: 0;
      }
      .cred-label {
        font-size: 10px; font-weight: 600;
        letter-spacing: 0.8px; text-transform: uppercase;
        color: #9ca3af; margin-bottom: 3px;
      }
      .cred-value {
        font-size: 15px; font-weight: 700; color: #111827;
      }
      .cred-value.mono {
        font-family: 'Courier New', monospace;
        background: rgba(229,57,53,0.07);
        border: 1px solid rgba(229,57,53,0.12);
        border-radius: 8px;
        padding: 4px 10px; display: inline-block; font-size: 14px;
      }
      .btn-wrap { text-align: center; margin-bottom: 28px; }
      .btn {
        display: inline-block;
        background: linear-gradient(135deg, #c62828 0%, #e53935 100%);
        color: #ffffff !important;
        text-decoration: none;
        font-size: 14px; font-weight: 700;
        padding: 14px 44px;
        border-radius: 50px;
        letter-spacing: 0.3px;
        box-shadow: 0 6px 22px rgba(198,40,40,0.38), inset 0 1px 0 rgba(255,255,255,0.18);
      }
      .notice {
        background: rgba(255,248,230,0.7);
        border: 1px solid rgba(251,191,36,0.2);
        border-radius: 12px;
        padding: 12px 16px;
        display: flex; gap: 10px; align-items: flex-start;
      }
      .notice-bar {
        width: 3px; background: #f59e0b;
        border-radius: 4px; flex-shrink: 0; align-self: stretch;
      }
      .notice-text { font-size: 13px; color: #78350f; line-height: 1.6; }
      .notice-text strong { color: #451a03; }
      hr.div { border: none; border-top: 1px solid rgba(0,0,0,0.06); margin: 28px 0; }
      .card-footer {
        padding: 20px 44px 28px;
        text-align: center;
        border-top: 1px solid rgba(0,0,0,0.05);
      }
      .footer-text { font-size: 12px; color: #9ca3af; line-height: 1.7; }
      .footer-text a { color: #e53935; text-decoration: none; }
      .brand {
        font-size: 11px; font-weight: 700;
        letter-spacing: 2px; text-transform: uppercase;
        color: #d1d5db; margin-top: 10px;
      }
  body
    .wrapper
      .card
        .card-stripe
        .card-header
          p.header-eyebrow Cuenta creada
          h1.header-title Bienvenido/a a
          h1.header-title
            span ASHA Solution
          p.header-sub Tu acceso ha sido configurado exitosamente
        .card-body
          p.greeting Hola, #{name}
          p.body-text
            | Tu cuenta en la plataforma ha sido creada. A continuación encontrarás
            | tus credenciales de acceso iniciales. Te recomendamos cambiar tu
            | contraseña en cuanto inicies sesión.
          .glass-box
            p.box-label Credenciales de acceso
            .cred-row
              .cred-dot
              div
                p.cred-label Correo electrónico
                p.cred-value #{email}
            .cred-row
              .cred-dot
              div
                p.cred-label Contraseña temporal
                p.cred-value.mono #{temporaryPassword}
          .btn-wrap
            a.btn(href=accessUrl target="_blank") Acceder a la plataforma
          hr.div
          .notice
            .notice-bar
            p.notice-text
              strong Contraseña temporal.&nbsp;
              | Por seguridad, cámbiala desde tu perfil en cuanto inicies sesión.
        .card-footer
          p.footer-text
            | Si no esperabas este correo, contáctanos en&nbsp;
            a(href="mailto:soporte@ashasolution.com") soporte@ashasolution.com
          p.brand ASHA Solution
